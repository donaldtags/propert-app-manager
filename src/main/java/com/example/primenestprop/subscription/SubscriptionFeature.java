package com.example.primenestprop.subscription;

/** Gated capabilities that switch on at Growth+ (see SubscriptionPlanSettings per-plan flags). */
public enum SubscriptionFeature {
    ESCROW, DIGITAL_LEASES, MAINTENANCE_REQUESTS, RENT_REMINDERS, AI_PRICING, TENANT_PASSPORT, REPORTS
}