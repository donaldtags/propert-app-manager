package com.example.primenestprop.subscription;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SubscriptionPlanSettingsRepository extends JpaRepository<SubscriptionPlanSettings, SubscriptionPlan> {
}